apiVersion: v1
kind: Service
metadata:
  name: admin-front-end-app-service
  labels:
    role: admin-front-end
spec:
  selector:
    role: admin-front-end
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
