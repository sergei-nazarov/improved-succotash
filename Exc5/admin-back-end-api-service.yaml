apiVersion: v1
kind: Service
metadata:
  name: admin-back-end-api-app-service
  labels:
    role: admin-back-end-api
spec:
  selector:
    role: admin-back-end-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
